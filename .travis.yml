language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules

install:
  - npm install -g codecov

script:
  - npm run build
  - codecov

after_success:
  - bash <(curl -s https://codecov.io/bash)

on:
  branch: master